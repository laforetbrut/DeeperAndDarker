# Claude Code
.claude/
CLAUDE.md
helper-build/
helper-stubs/
asm_patcher/*.class
asm_patcher/*.original.class

# Test procedure artefacts (local QA only — never push)
test-procedures/
TEST_PROCEDURE_*.html
